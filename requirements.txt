# Project APE - Python Dependencies
# Requires Python 3.11+

# ==============================================================================
# GOOGLE DRIVE INTEGRATION (Required)
# ==============================================================================
# Core Google Drive API for downloading client documents
google-api-python-client>=2.140.0
google-api-core>=2.19.0
google-auth>=2.30.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# ==============================================================================
# GOOGLE GEMINI AI (Optional - For AI Orchestration)
# ==============================================================================
# Gemini AI for intelligent error recovery and quality validation
google-genai>=2.0.0

# ==============================================================================
# WEB DASHBOARD (Required)
# ==============================================================================
# Flask web server for real-time progress monitoring
flask>=3.0.0

# Waitress WSGI server - production-grade replacement for Flask dev server
# Critical for deep mode: handles 100+ concurrent connections (SSE streams + polling)
# Flask dev server struggles with thread exhaustion during long-running workflows
waitress>=3.0.0
werkzeug>=3.0.0

# ==============================================================================
# PDF & IMAGE PROCESSING (Optional - Only for Gemini Agent Mode)
# ==============================================================================
# PDF consolidation and image-to-PDF conversion
# Note: Only used when GEMINI_API_KEY is set and Gemini agent mode is enabled
# Standard execution (default) does NOT use these - it adds individual files
# You can remove these if you never use Gemini agent orchestration mode
pypdf>=4.0.0
Pillow>=10.0.0

# ==============================================================================
# CONFIGURATION MANAGEMENT (Required)
# ==============================================================================
# Load environment variables from .env file
python-dotenv>=1.0.0

# ==============================================================================
# UPDATE SYSTEM (No Additional Dependencies Required)
# ==============================================================================
# Phase 1: Force Refresh - Uses existing google-api-python-client
# Phase 2: UpdateManager - Uses only Python standard library (pathlib, typing, etc.)
# Phase 3: Not implemented
#
# All update functionality uses existing dependencies above.
# No additional packages needed.

# ==============================================================================
# TESTING (Development Only)
# ==============================================================================
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.12.0

# ==============================================================================
# SECURITY (Required)
# ==============================================================================
flask-wtf>=1.2.0
